version: '3.8'
services:
  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - BROWSER_POOL_SIZE=2
    restart: unless-stopped
    shm_size: '256mb'
